FROM node:18-alpine

WORKDIR /app

# 复制后端package.json并安装依赖
COPY backend/package.json backend/package-lock.json* ./
RUN npm ci --only=production

# 复制后端代码
COPY backend/ ./

# 复制server.js（在根目录）
COPY server.js ./

# 暴露端口
EXPOSE 3000

# 启动命令
CMD ["node", "server.js"]

